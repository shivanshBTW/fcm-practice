import React from 'react';
import {sendMessage} from "./sendNotification";
// import handleFirebaseConfiguration from "./FirebaseConfig";
//
// handleFirebaseConfiguration();
// setInterval(sendMessage,6000);
function App() {
   return (
      <div className="App">
         lol
         {/*<button onClick={sendMessage}>lol</button>*/}
      </div>
   );
}

export default App;
